/* ...existing imports... */
import TypewriterText from '../components/TypewriterText';

const AIPage = () => {
  /* ...existing code... */

  return (
    /* ...existing code... */
    <div className="analysis-content">
      <TypewriterText text={aiResponse} speed={10} />
    </div>
    /* ...existing code... */
  );
};

export default AIPage;
